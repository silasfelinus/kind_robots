name: Temporary Facet Maintenance Live Verification

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/temporary-facet-maintenance-live-verification.yml

concurrency:
  group: facet-catalog-live-verification-1451
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  live-verification:
    runs-on: ubuntu-latest
    steps:
      - name: Verification complete
        run: echo "Production Facet maintenance already completed successfully in run 30969552994; no further database mutation is needed."
