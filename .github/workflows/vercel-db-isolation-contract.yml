name: Vercel DB Isolation Contract

on:
  pull_request:
    branches: [main]
    paths:
      - 'scripts/provision-vercel-db-isolation.sh'
      - 'scripts/provision-agent-db-lane.sh'
      - 'scripts/prisma-migrate-deploy.mjs'
      - 'utils/scripts/verifyVercelDbIsolationProvisioning.mjs'
      - 'utils/scripts/verifyAgentDbLaneProvisioning.mjs'
      - 'docs/runbooks/agent-database-lane.md'
      - '.github/workflows/vercel-db-isolation-contract.yml'
  workflow_dispatch:

concurrency:
  group: vercel-db-isolation-contract-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Provisioning contract
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Verify Vercel provisioning script
        run: node utils/scripts/verifyVercelDbIsolationProvisioning.mjs

      - name: Verify coding-agent provisioning script
        run: node utils/scripts/verifyAgentDbLaneProvisioning.mjs
