name: Thin Dream Store Codemod

on:
  push:
    branches:
      - refactor/thin-resource-apis
    paths:
      - '.github/workflows/thin-dream-store-codemod.yml'

permissions:
  contents: write

jobs:
  apply:
    if: contains(github.event.head_commit.message, 'run Dream store codemod')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/thin-resource-apis
          fetch-depth: 0

      - uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Apply guarded codemod
        run: node scripts/refactor-thin-dream-store.mjs

      - name: Commit transformed files
        run: |
          git config user.name "kind-robots-refactor"
          git config user.email "kind-robots-refactor@users.noreply.github.com"
          git add stores/dreamStore.ts components/dreams/dream-maker.vue
          git diff --staged --quiet && exit 1
          git commit -m "refactor(dreams): coordinate lean mutations in store [skip ci]"
          git push origin HEAD:refactor/thin-resource-apis
